#include <bits/stdc++.h>
using namespace std;
int main()
{
    time_t timer = time(NULL);
    struct tm *l = localtime(&timer);
    printf("%d-%02d-%02d", l->tm_year + 1900, l->tm_mon + 1, l->tm_mday);
    return 0;
}